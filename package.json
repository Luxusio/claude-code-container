{
  "name": "claude-code-container",
  "version": "1.0.0",
  "description": "Run Claude Code in isolated Docker containers",
  "type": "module",
  "bin": {
    "ccc": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build",
    "install:global": "node scripts/install.js",
    "uninstall:global": "node scripts/install.js --uninstall"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "claude",
    "container",
    "sandbox",
    "docker",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/claude-code-container"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.0.18",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.17"
  }
}
